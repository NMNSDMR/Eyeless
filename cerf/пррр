# Проверка чисел в интервале от 1000000 до 1000100
for num in range(1000000, 1000101):
    count = 0
    for i in range(1, int(num**0.5) + 1):
        if num % i == 0:
            # Если i и num // i разные, то у числа есть два делителя
            if i != num // i:
                count += 2
            else:
                count += 1
    if count == 6:
        print(num)