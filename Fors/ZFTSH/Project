# Входное значение
Y = 1234.56789

# Преобразуем Y в строку
Y_str = str(Y)

# Найдем позицию десятичной точки
decimal_point_position = Y_str.find('.')

# Получим подстроку, начиная с позиции десятичной точки
decimal_part = Y_str[decimal_point_position + 1:]

# Извлечем первые четыре цифры из десятичной части
P = int(decimal_part[:4])

# Выведем значение P
print(P)
